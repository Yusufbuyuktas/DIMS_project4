spring.application.name=proje-backend
server.port=8080

# PostgreSQL Ba?lant? Ayarlar?
spring.datasource.url=jdbc:postgresql://localhost:5432/final_proje_db
spring.datasource.username=postgres
spring.datasource.password=yusuf0007

# Hibernate/JPA Ayarlar? (Tablolar? otomatik olu?turur)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Dosya Yükleme S?n?rlar? (Resim yükleme için laz?m)
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB